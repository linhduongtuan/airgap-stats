"""Executable tooling for the DR workflow pack (as opposed to ``skills/``,
which is instructions for an agent, and ``r-scripts`` / ``py-scripts`` /
``projects/*/scripts``, which are the generated analysis code)."""
